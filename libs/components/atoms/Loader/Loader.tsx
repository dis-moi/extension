import React from 'react';
import LoaderWrapper from './LoaderWrapper';
import { LoadingBig } from 'libs/components/atoms/icons';

const Loader = () => (
  <LoaderWrapper>
    <LoadingBig />
  </LoaderWrapper>
);

export default Loader;
