import styled from 'styled-components';
import ContentWrapper from './ContentWrapper';

const contentWrapperBackground = styled(ContentWrapper)`
  height: calc(100% - 20px);
  margin: 10px 13px;
  background-color: #fff;
  border-radius: 8px;
`;

export default contentWrapperBackground;
