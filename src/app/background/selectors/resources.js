export const getMatchingContexts = state => state.get('resources').get('matchingContexts').toJS();

export const getDrafRecommendations = state => state.get('resources').get('draftRecommendations').toJS();
