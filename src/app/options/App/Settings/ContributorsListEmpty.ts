import styled from 'styled-components';

const ContributorsListEmpty = styled.p`
  text-align: center;
  font-size: 18px;
  color: ${props => props.theme.primaryColor};
`;

export default ContributorsListEmpty;
