import styled from 'styled-components';

export default styled.h2`
  margin: 22px 0 0;
  font-size: 22px;
  font-weight: bold;
  color: ${props => props.theme.secondaryColor};
  text-align: center;
`;
