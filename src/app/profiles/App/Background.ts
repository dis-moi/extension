import styled from 'styled-components';

const Background = styled.div`
  background-color: ${props => props.theme.noticeBg};

  ////////////////////////////////////////////
  margin: -8px; // TO DELETE when on WordPress
  ////////////////////////////////////////////
`;

export default Background;
