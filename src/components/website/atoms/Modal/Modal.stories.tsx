import React from 'react';
import Modal, { ModalProps } from './Modal';

export default {
  title: 'Website/Atoms/Modal',
  argTypes: {}
};

export const _Modal = (args: ModalProps) => <Modal {...args} />;
