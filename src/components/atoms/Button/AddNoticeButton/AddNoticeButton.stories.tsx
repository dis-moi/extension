import React from 'react';
import { storiesOf } from '@storybook/react';
import AddNoticeButton from './AddNoticeButton';

storiesOf('atoms/Button/AddNoticeButton', module).add('normal', () => (
  <AddNoticeButton />
));
