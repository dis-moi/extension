import React from 'react';
import { storiesOf } from '@storybook/react';
import AddNoticeButton from './AddNoticeButton';

storiesOf('atoms/Buttons/AddNoticeButton', module).add('normal', () => (
  <AddNoticeButton />
));
