import React from 'react';
import OpenButton from './OpenButton';

export default {
  title: 'Components/Atoms/Buttons/Open'
};

export const _OpenButton = () => <OpenButton />;

_OpenButton.story = {
  name: 'OpenButton'
};
