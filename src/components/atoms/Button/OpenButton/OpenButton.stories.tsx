import React from 'react';
import { storiesOf } from '@storybook/react';
import OpenButton from './OpenButton';

storiesOf('atoms/Button/OpenButton', module).add('normal', () => (
  <OpenButton />
));
