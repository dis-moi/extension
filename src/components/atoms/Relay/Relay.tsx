import styled from 'styled-components';

const RelayPart = styled.div`
  display: flex;
  flex: 1;
  align-items: center;
  font-size: 12px;

  & > svg {
    width: 16px;
    height: auto;
    margin-right: 6px;
  }
`;

export default RelayPart;
