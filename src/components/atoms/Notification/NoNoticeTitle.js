import styled from 'styled-components';

export default styled.h2`
    margin: 12px 0 26px;
    font-size: 22px;
    font-weight: bold;
    color: ${props => props.theme.secondaryColor};
    text-align: center;
`;